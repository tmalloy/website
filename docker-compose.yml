version: '2'
services:

    www:
        build: ./image
        networks:
            global:
              aliases:
                - website

networks:
    global:
        external:
            name: global


